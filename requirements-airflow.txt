# ML deps for Airflow workers (Dockerfile.airflow). Keep separate from requirements.txt
# to avoid clashing with Apache Airflow’s pinned transitive dependencies.
pandas
numpy
scikit-learn
matplotlib
seaborn
# Must match MLflow tracking server image (Dockerfile.mlflow); 3.x client calls APIs 2.x server lacks
mlflow==2.17.2
pyyaml
joblib
pyarrow
fastparquet
